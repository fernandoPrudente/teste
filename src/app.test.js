const expect = require('chai').expect;

describe('teste 01', () => {
    it('tem q ser verdadeiro', () => {
        expect(1).eq(1);
    });
    it('novo teste', () => {
        expect(2).eq(2);
    });    
});